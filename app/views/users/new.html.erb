<div class="container">
  <div class="row">
    <div class="col-6">
      <h1>Crear Cuenta</h1>

      <% if @user.errors.any? %>
        <div class="alert alert-danger">
          <ul>
            <% @user.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <%= form_for @user do |f| %>
        <div class="form-group">
          <%= label_tag(:email, '<h5><i class="fas fa-at"></i>  Ingresa tu E-mail</h5>'.html_safe) %>
          <%= f.email_field :email, class: "form-control input-lg", placeholder: "Email", placeholder: "Correo electrónico" %>
        </div>

        <div class="form-group">
          <%= label_tag(:email, '<h5><i class="fas fa-at"></i>  Ingresa una contraseña</h5>'.html_safe) %>
          <%= f.password_field :password, class: "form-control input-lg", placeholder: "Contraseña", :autocomplete => :off, placeholder: "Contraseña" %>
        </div>

        <div class="form-group">
          <%= label_tag(:email, '<h5><i class="fas fa-user"></i>  Ingresa un nombre de cuenta</h5>'.html_safe) %>
          <%= f.text_field :name, class: "form-control input-lg", placeholder: "Nombre", :autocomplete => :off, placeholder: "Nombre" %>
        </div>

        <div class="actions">
          <%= f.submit "Crear cuenta", class: "btn btn-primary btn-block btn-lg" %>
        </div>
      <% end %>
      <br>
      <div class="row">
        <div class="col-12">
          <%= link_to "Ya tienes cuenta?", login_path, class: "btn btn-lg pull-left", id: "nuevo" %>
        </div>
      </div>

    </div>
  </div>
</div>
